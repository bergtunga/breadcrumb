// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCksXBWA978fCx1wlSSUwDpgfr-5vFEIGU",
    authDomain: "bvt-project-backend.firebaseapp.com",
    projectId: "bvt-project-backend",
    storageBucket: "bvt-project-backend.appspot.com",
    messagingSenderId: "48845040822",
    appId: "1:48845040822:web:5243067845c6802cc6ca17"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
